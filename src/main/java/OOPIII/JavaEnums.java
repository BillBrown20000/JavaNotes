package OOPIII;

public enum JavaEnums {

    GOOD, BAD, UNKNOWN
}
/*
In Java, an enum (short for enumeration) is a
type that has a fixed set of possible values.
We use the enum keyword to declare enums.

An enum class can include methods and fields just like regular classes.

 */
